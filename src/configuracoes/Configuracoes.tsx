import { Grid, Typography } from '@mui/material';

import { LayoutBase } from '../shared/layouts';

export const Configuracoes = () => {

    return (
        <LayoutBase>
            <Grid>
                <Grid display='flex' justifyContent='center'>
                    <Typography fontWeight='bold' variant='h3'>WIP</Typography>
                </Grid>
            </Grid>
        </LayoutBase>
    );
};